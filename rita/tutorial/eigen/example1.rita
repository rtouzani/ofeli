# Iris Script file to practice eigenproblem solver
# The matrix is defined entry by entry
#
  A = matrix(2,2)
  A[0,0]=2
  A[0,1]=-1
  A[1,0]=-1
  A[1,1]=2

# Define eigenvalue problem, eigenvectors are not computed
eigen matrix=A eigv

# Solve the defined problem
solve

# Print eigenvalues
= Aev_R
exit
