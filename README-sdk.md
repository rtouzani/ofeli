The script install.sh helps you installing ofeli files.
By default the installation is in the folder: '/usr/local/'
You can change this by modifying the value of PREFIX.

Note that using this default prefix requires administrator privileges.
You can then execute the script 'install.sh' to install all library components.
